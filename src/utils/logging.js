export const group = console.group ? console.group : console.log;
export const groupCollapsed = console.groupCollapsed ? console.groupCollapsed : console.log;
export const groupEnd = console.groupEnd ? console.groupEnd : () => {};
